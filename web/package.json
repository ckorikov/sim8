{
  "name": "sim8-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p ../docs && esbuild src/sim.js --bundle --format=iife --outfile=../docs/sim.bundle.js --sourcemap && cp index.html sim.css ../docs/",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "puppeteer-core": "^24.0.0"
  }
}
